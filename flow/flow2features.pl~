#!/usr/bin/perl -w
use strict;

my $outputDir = shift or die;
my $domain = shift or die;

open LS, "ls output/$domain.[0-9]* |" or die;
while (my $fname = <LS>) {
    chomp $fname;

    
}
close LS;


open O, "> $outputDir/$domain.type.hal-flow" or die;
